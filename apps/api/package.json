{
    "name": "api",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": ".\\venv\\Scripts\\uvicorn index:app --reload --port 8000",
        "start": ".\\venv\\Scripts\\uvicorn index:app --host 0.0.0.0 --port 8000",
        "lint": "echo 'No linting configured for Python'"
    }
}